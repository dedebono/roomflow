# Database
DATABASE_URL="postgresql://roomflow:roomflow@localhost:5432/roomflow?schema=public"

# JWT
JWT_SECRET="roomflow-super-secret-key-123!"

# Server
PORT=3000

# Storage (LOCAL | S3 | GOOGLE_DRIVE)
STORAGE_TYPE=LOCAL

# Email (optional - set to "true" to enable real email sending)
EMAIL_ENABLED=false
EMAIL_FROM=noreply@roomflow.local
